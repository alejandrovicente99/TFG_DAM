package UI;

import ORM.Libreria;
import Servicios.LibreriaDataService;
import org.hibernate.Session;

public class Individual {
    /*public Individual(Session session) {this.session = session;}
    private Session session;

    Scrap.Scrap s = new Scrap.Scrap();
    LibreriaDataService l = new LibreriaDataService(session);

    public String puntuacionMetacritic(String url){
        return s.webScrap(url).get(0);
    }
    public String puntuacionIMDB(String url){
        return s.webScrap(url).get(1);
    }
    public String imagenIMDB(String url){
        return s.webScrap(url).get(2);
    }
    public String nombre(Libreria libreria) {
        return libreria.getNombre();
    }
    public String tipo(Libreria libreria) {
        return libreria.getTipo();
    }
    public String fecha(Libreria libreria) {
        return libreria.getFechaFin();
    }
    public int puntuacion(Libreria libreria) {
        return libreria.getPuntuacion();
    }
    public int ranking(Libreria libreria) {
        return l.ranking(libreria.getNombre());
    }*/
}
